gdpr_replacements:
  users_field_data:
    name:
      formatter: 'username'
    mail:
      formatter: 'email'
    pass:
      formatter: 'clear'
# TODO: move this to enum class.
drivers:
  mysql:
    dump_command: 'mysqldump'
  oracle:
    dump_command: 'mysqldump'
  pqsql:
    dump_command: 'pg_dump'
  sqlite:
    dump_command: 'dump'
  sqlsrv:
    dump_command: 'mysqldump'